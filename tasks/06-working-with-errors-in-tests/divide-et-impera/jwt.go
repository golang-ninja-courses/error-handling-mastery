package jwt

func parseHeader(data []byte) (h Header, err error) {
	// Реализуй меня.
	return Header{}, nil
}

func parsePayload(data []byte) (t Token, err error) {
	// Реализуй меня.
	return Token{}, nil
}
